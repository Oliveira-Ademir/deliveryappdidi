import 'package:flutter/material.dart';

class CardViewButtonTest extends StatefulWidget {
  const CardViewButtonTest({Key? key}) : super(key: key);

  @override
  State<CardViewButtonTest> createState() => _CardViewButtonTestState();
}

class _CardViewButtonTestState extends State<CardViewButtonTest> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Column(
          children: const [

          ],
        ),
      ),
    );
  }
}
